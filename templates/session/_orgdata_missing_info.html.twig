{% if not is_granted('ROLE_EDITOR') and (app.user.proposedOrganizationDetails is null or app.user.proposedOrganizationDetails.title == "") %}
    <div class="alert alert-warning" role="alert">
        <p>
            Aktuell sind die Veranstalterdaten nicht, oder nicht vollständig, erfasst. Diese müssen gepflegt werden,
            ehe eine Session veröffentlicht werden kann.
        </p>

        <a class="btn btn-warning" href="{{ path('my_organization') }}">Veranstalterangaben pflegen</a>
    </div>
{% endif %}