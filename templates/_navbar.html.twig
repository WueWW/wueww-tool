<nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
    <a href="{{ path('welcome') }}"><span class="navbar-brand mb-0 h1">Utes Helferlein</span></a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li>
                <a class="nav-link" href="{{ path('session_index') }}">Events</a>
            </li>
            {% if app.user %}
                {% if is_granted('ROLE_EDITOR') %}
                    <li>
                        <a class="nav-link" href="{{ path('organization_index') }}">Veranstalter</a>
                    </li>
                {% else %}
                    <li>
                        <a class="nav-link" href="{{ path('my_organization') }}">Veranstalterangaben</a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_USER_APPROVER') %}
                    <li>
                        <a class="nav-link" href="{{ path('user_approval_index') }}">Neubenutzer</a>
                    </li>
                    {% endif %}
            {% endif %}
        </ul>
        {% if app.user %}
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false">
                        {{ app.user.email }}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a>
                    </div>
                </li>
            </ul>
        {% endif %}
    </div>
</nav>