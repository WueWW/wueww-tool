{% extends 'narrow.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Passwort zurücksetzen</button>
    {{ form_end(form) }}
{% endblock %}
